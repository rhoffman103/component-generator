import React, { Component } from "react";
// import PropTypes from "propt-types";

class {{ title }} extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  componentDidMount() {};

  render() {
    return (
      <div>{{ title }} Class Component!</div>
    );
  };
}

{{ title }}.propTypes = {};

export default {{ title }}